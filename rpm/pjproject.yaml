Name: pjproject
Summary: A multimedia communication library implementing standard based protocols such as SIP, SDP, RTP, STUN, TURN, and ICE
Version: 2.11.1
Group: Development/Libraries
URL: https://www.pjsip.org/
SCM: https://github.com/pjsip/pjproject
License: GPLv2
Sources:
    - '%{name}-%{version}.tar.gz'
#Requires:
#    - ^^^
PkgBR:
    - opus-devel
PkgConfigBR:
    - speex
    - speexdsp
    - openssl
SetupOptions: '-q -n %{name}-%{version}/upstream'
ConfigOptions:
    - '--enable-shared'
    - '--disable-video'
    - '--enable-opus'
    - '--enable-speex'
    - '--enable-ssl'
    - 'CFLAGS="$CFLAGS -DNDEBUG=1"'
    - 'CFLAGS="$CFLAGS -DPJ_HAS_IPV6=1"'
Builder: none
AutoSubPackages:
    - devel
